{% extends 'base.html.twig' %}

{% block title %}Hello ProjectController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class='container4'>
    <div class='tableShow '>
        <div class="example-wrapper">
            <h1>Hello {{ app.user.firstName }} ! </h1>
            
                <div class="row">
                    <div class="column">
                        <table class="u-full-width">
                            <thead>
                                <tr>
                                    <th>Titre</th>
                                    <th>Date</th>
                                    <th>Montant</th>
                                    <th>Fait par</th>
                                    <th>Supprimer</th>
                                    
                                </tr>
                            </thead>
                            <tbody>
                                {% for expense in project.getExpenses %}
                                <tr class='formline'>
                                    
                                    <td>{{ expense.getTitle }}</td>
                                    <td>{{ expense.getDate|date("d-M-Y") }}</td>
                                    <td>{{ expense.getAmount}} €</td>
                                    <td>{{ expense.getUser }}</td>
                                    <td><a href="{{ path('expense_delete', {"id":project.id, "expense_id":expense.id})}}"  class=" button">Supprimer</a></td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                
                </div>
                
            </form>
            
            <div class="logdivbtn">
                <a class="button " href="{{ path('projects_addExpense', {'id': project.id}) }}">Ajouter</a>
                <a class="button " href="{{ path('project_show', {'id': project.id}) }}">Retour</a>
            </div>
        
        </div>
    </div>
</div>
{% endblock %}
