{% extends 'base.html.twig' %}

{% block title %}Add users{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class='container4'>
  <div class='projectCard addUser'>
    <h1>Ajouter un utilisateur au projet</h1>
     {{ form_start(form) }}
        {{ form_label(form.email) }}
        {{ form_widget(form.email) }}
     {{ form_end(form) }}

    {% for user in projectUsers %}
    <div class='listUser'>  
        <a>{{ user.getFirstName }} {{user.getLastName }}</a>
        
        
    </div>
    {% endfor %} 
    
     <a class="button button-primary" href="{{ path('project_show', {'id': project.id}) }}">Retour</a>
                
       

   </div>
</div>
{% endblock %}
